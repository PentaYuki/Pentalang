version: '3.8'

services:
  app:
    image: gooleseswsq11/pentalang:latest
    container_name: kotoba-pentalang
    environment:
      - DISPLAY=host.docker.internal:0
      - QT_X11_NO_MITSHM=1
    volumes:
      - ./data:/app/data
      - ./voice:/app/voice
      - ./models:/app/models
    network_mode: "host"
    privileged: true
    restart: unless-stopped
